{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="float-end">
        <button type="submit" form="form-module" data-bs-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa-solid fa-save"></i></button>
        <a href="{{ back }}" data-bs-toggle="tooltip" title="{{ button_back }}" class="btn btn-light"><i class="fa-solid fa-reply"></i></a></div>
      <h1>{{ heading_title }}</h1>
      <ol class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
          <li class="breadcrumb-item"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ol>
    </div>
  </div>
  <div class="container-fluid">
    <div class="card">
      <div class="card-header"><i class="fa-solid fa-pencil"></i> {{ text_edit }}</div>
      <div class="card-body">
        <form id="form-module" action="{{ save }}" method="post" data-oc-toggle="ajax">
          <div class="row mb-3">
            <label for="input-name" class="col-sm-2 col-form-label">{{ entry_name }}</label>
            <div class="col-sm-10">
              <input type="text" name="name" value="{{ name }}" placeholder="{{ entry_name }}" id="input-name" class="form-control"/>
              <div id="error-name" class="invalid-feedback"></div>
            </div>
          </div>
          <div class="row mb-3">
            <label for="input-status" class="col-sm-2 col-form-label">{{ entry_status }}</label>
            <div class="col-sm-10">
              <select name="status" id="input-status" class="form-select">
                <option value="1"{% if status %} selected="selected"{% endif %}>{{ text_enabled }}</option>
                <option value="0"{% if not status %} selected="selected"{% endif %}>{{ text_disabled }}</option>
              </select>
            </div>
          </div>

          <fieldset>
            <legend>Services</legend>
            <table id="service" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <td class="text-start">{{ entry_title }}</td>
                  <td class="text-start">{{ entry_description }}</td>
                  <td class="text-start">{{ entry_tooltip }}</td>
                  <td class="text-center">{{ entry_image }}</td>
                  <td></td>
                </tr>
              </thead>
              <tbody>
                {% set service_row = 0 %}
                {% for service in services %}
                  <tr id="service-row-{{ service_row }}">
                    <td class="text-start">
                      {% for language in languages %}
                        <div class="input-group mb-1">
                          <span class="input-group-text"><img src="{{ language.image }}" title="{{ language.name }}"/></span>
                          <input type="text" name="services[{{ service_row }}][title][{{ language.language_id }}]" value="{{ service.title[language.language_id] }}" placeholder="{{ entry_title }}" class="form-control" />
                        </div>
                      {% endfor %}
                    </td>
                    <td class="text-start">
                      {% for language in languages %}
                        <div class="input-group mb-1">
                          <span class="input-group-text"><img src="{{ language.image }}" title="{{ language.name }}"/></span>
                          <input type="text" name="services[{{ service_row }}][description][{{ language.language_id }}]" value="{{ service.description[language.language_id] }}" placeholder="{{ entry_description }}" class="form-control" />
                        </div>
                      {% endfor %}
                    </td>
                    <td class="text-start">
                      {% for language in languages %}
                        <div class="input-group mb-1">
                          <span class="input-group-text"><img src="{{ language.image }}" title="{{ language.name }}"/></span>
                          <textarea name="services[{{ service_row }}][tooltip][{{ language.language_id }}]" rows="2" placeholder="{{ entry_tooltip }}" class="form-control">{{ service.tooltip[language.language_id] }}</textarea>
                        </div>
                      {% endfor %}
                    </td>
                    <td class="text-center">
                    <div class="card image">
                      <img src="{{ service.thumb }}" alt="" title="" id="thumb-image-{{ service_row }}" data-oc-placeholder="{{ placeholder }}" class="card-img-top"/> <input type="hidden" name="services[{{ service_row }}][image]" value="{{ service.image }}" id="input-image-{{ service_row }}"/>
                      <div class="card-body">
                        <button type="button" data-oc-toggle="image" data-oc-target="#input-image-{{ service_row }}" data-oc-thumb="#thumb-image-{{ service_row }}" class="btn btn-primary btn-sm btn-block"><i class="fa-solid fa-pencil"></i> {{ button_edit }}</button>
                        <button type="button" data-oc-toggle="clear" data-oc-target="#input-image-{{ service_row }}" data-oc-thumb="#thumb-image-{{ service_row }}" class="btn btn-warning btn-sm btn-block"><i class="fa-regular fa-trash-can"></i> {{ button_clear }}</button>
                      </div>
                    </div>
                    </td>
                    <td class="text-end"><button type="button" onclick="$('#service-row-{{ service_row }}').remove();" data-bs-toggle="tooltip" title="{{ button_remove }}" class="btn btn-danger"><i class="fa-solid fa-minus-circle"></i></button></td>
                  </tr>
                  {% set service_row = service_row + 1 %}
                {% endfor %}
              </tbody>
              <tfoot>
                <tr>
                  <td colspan="4"></td>
                  <td class="text-end"><button type="button" onclick="addservice();" data-bs-toggle="tooltip" title="{{ button_service_add }}" class="btn btn-primary"><i class="fa-solid fa-plus-circle"></i></button></td>
                </tr>
              </tfoot>
            </table>
          </fieldset>
        </form>
      </div>
    </div>
  </div>
</div>
<script type="text/javascript"><!--
var service_row = {{ service_row }};

function addservice() {
	var html  = '<tr id="service-row-' + service_row + '">';
    html += '  <td class="text-start">';
    {% for language in languages %}
    html += '    <div class="input-group mb-1">';
    html += '      <span class="input-group-text"><img src="{{ language.image }}" title="{{ language.name }}"/></span>';
    html += '      <input type="text" name="services[' + service_row + '][title][{{ language.language_id }}]" value="" placeholder="{{ entry_title|escape('js') }}" class="form-control" />';
    html += '    </div>';
    {% endfor %}
    html += '  </td>';
	html += '  <td class="text-start">';
    {% for language in languages %}
    html += '    <div class="input-group mb-1">';
    html += '      <span class="input-group-text"><img src="{{ language.image }}" title="{{ language.name }}"/></span>';
    html += '      <input type="text" name="services[' + service_row + '][description][{{ language.language_id }}]" value="" placeholder="{{ entry_description|escape('js') }}" class="form-control" />';
    html += '    </div>';
    {% endfor %}
    html += '  </td>';
    html += '  <td class="text-start">';
    {% for language in languages %}
    html += '    <div class="input-group mb-1">';
    html += '      <span class="input-group-text"><img src="{{ language.image }}" title="{{ language.name }}"/></span>';
    html += '      <textarea name="services[' + service_row + '][tooltip][{{ language.language_id }}]" rows="2" placeholder="{{ entry_tooltip|escape('js') }}" class="form-control"></textarea>';
    html += '    </div>';
    {% endfor %}
    html += '  </td>';
	html += '  <td class="text-center">';
    html += '  <div class="card image">';
    html += '    <img src="{{ placeholder }}" alt="" title="" id="thumb-image-' + service_row + '" data-oc-placeholder="{{ placeholder }}" class="card-img-top"/> <input type="hidden" name="services[' + service_row + '][image]" value="" id="input-image-' + service_row + '"/>';
    html += '    <div class="card-body">';
    html += '      <button type="button" data-oc-toggle="image" data-oc-target="#input-image-' + service_row + '" data-oc-thumb="#thumb-image-' + service_row + '" class="btn btn-primary btn-sm btn-block"><i class="fa-solid fa-pencil"></i> {{ button_edit|escape('js') }}</button>';
    html += '      <button type="button" data-oc-toggle="clear" data-oc-target="#input-image-' + service_row + '" data-oc-thumb="#thumb-image-' + service_row + '" class="btn btn-warning btn-sm btn-block"><i class="fa-regular fa-trash-can"></i> {{ button_clear|escape('js') }}</button>';
    html += '    </div>';
    html += '  </div>';
    html += '  </td>';
	html += '  <td class="text-end"><button type="button" onclick="$(\'#service-row-' + service_row  + '\').remove();" data-bs-toggle="tooltip" title="{{ button_remove|escape('js') }}" class="btn btn-danger"><i class="fa-solid fa-minus-circle"></i></button></td>';
	html += '</tr>';

	$('#service tbody').append(html);

	service_row++;
}
//--></script>
{{ footer }}
