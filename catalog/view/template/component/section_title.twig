<div class="dc-section-title">
  <div class="container d-flex justify-content-between align-items-center">
    <h2>{{ title }}</h2>
    {% if link %}
      <a href="{{ link }}" class="view-all">{{ text_view_all }} <i class="fa fa-angle-right"></i></a>
    {% endif %}
  </div>
</div>
